@model DynaxInvoice.BO.DynaxFacility

@{
    ViewBag.Title = "Dynax Solutions Pvt. Ltd.";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}
<div class="homecontainer">
    <h3>Welcome to Booking Master</h3>
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        @{
            if (ViewBag.Status != null)
            {
                if (ViewBag.Status == 0)
                {
                    <div class="alert alert-success">
                        One Facility added successfully.
                    </div>
                }
                else if (ViewBag.Status == 1)
                {
                    <div class="alert alert-danger">
                        Something went wrong !! Please try after sometime.
                    </div>
                }
            }
        }
    </div>
    <div class="login bg-light">
        <center>
            <h3><i class="fas fa-globe"></i> Update Facility</h3>
        </center>
        <hr />
        @using (Html.BeginForm("Edit", "Dfacility", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                <div class="input-group">
                    <div class="input-group-prepend">
                        <div class="input-group-text"><i class="fas fa-cocktail"></i></div>
                    </div>
                    @Html.TextBoxFor(m => m.Facility, new { @class = "form-control", placeholder = "Facility", required = "required" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-xs-12">
                    @Html.CheckBoxFor(m => m.Status, new { @class = "with-font" })
                    <label for="Status">Active</label>
                </div>
            </div>
            <div class="form-group">
                <button class="btn btn-info m-b-20 btn-block text-uppercase waves-effect waves-light" type="submit">
                    <i class="fas fa-plus-square"></i> SAVE FACILITY
                </button>
            </div>
        }
    </div>
</div>
